{
  "name": "backend",
  "version": "1.0.0",
  "description": "Aurellion Backend",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rimraf dist && npx tsc",
    "prestart": "npm run build",
    "start": "node --experimental-specifier-resolution=node dist/index.js",
    "predev": "npm run build",
    "dev": "concurrently \"npx tsc -w\" \"nodemon --experimental-specifier-resolution=node dist/index.js\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.15",
    "@types/node": "^18.11.18",
    "@types/node-fetch": "^2.6.2",
    "concurrently": "^7.6.0",
    "express": "^4.18.2",
    "node-fetch": "^3.3.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
